import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-and-angular-standalone-v17',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
